{
    "name": "usafacts-assignment",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "workspaces": [
        "api",
        "app"
    ],
    "dependencies": {
        "axios": "^1.4.0"
    }
}
